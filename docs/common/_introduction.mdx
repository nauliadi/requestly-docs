export const Title = (props) => {
    switch(props.platform) {
        case "browser-extension":
            return "Introduction to Browser Extension"
        default:
            return "Introduction"
    }
}


<span>Hello {props.name}</span>

This is text some content from `_markdown-partial-example.mdx`.